const INITIAL_STATE = {
  packages: [
    'React',
    'Redux',
    'React-Router-v4'
  ],
};

const Login = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default Login;